/**
 * @extport/sdk — open-source license verification SDK for browser extensions.
 *
 * Phase 2 (Licensing). Will provide:
 *   - offline activation-code verification against the tenant's Ed25519 public key
 *   - online activate / heartbeat calls, MV3 service-worker compatible
 *
 * Intentionally empty until the Licensing milestone starts (see spec §5.4).
 */
export {}
