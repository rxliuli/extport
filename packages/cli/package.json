{
  "name": "@extport/cli",
  "version": "0.0.0",
  "private": true,
  "description": "CLI for the extport extension publishing platform",
  "type": "module",
  "bin": {
    "extport": "./dist/cli.mjs"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsdown",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@clack/prompts": "^1.7.0",
    "@types/node": "^26.1.1",
    "citty": "^0.2.2",
    "tsdown": "^0.22.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
